import React, { Component } from 'react'

export default class Disclaimer extends Component {

    constructor(props) {
        super(props)
    
        this.state = {
             
        }
    }
    
    render() {
        return (
            <div style={{textAlign : 'center', fontSize : '14px', backgroundColor:'black', color : 'white', width : '100vw', position : 'fixed', bottom : '0px'}}>
             sync -> collect // sync -> mint // sync -> curate   
            </div>
        )
    }
}
