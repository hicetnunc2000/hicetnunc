import React from 'react'

const Footer = () => {
  return (
    <div
      style={{
        position: 'fixed',
        left: '0',
        bottom: '0',
        width: '100%',
        backgroundColor: 'black',
        textAlign: 'center',
        fontSize: '14px',
        color: 'white',
      }}
    >
      sync -&gt; collect // sync -&gt; mint // sync -&gt; curate
    </div>
  )
}

export default Footer
