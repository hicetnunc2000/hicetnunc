import { Feeds } from './feeds'

export const Latest = () => {
  return <Feeds type={1} />
}

export const Hdao = () => {
  return <Feeds type={1} />
}

export const Random = () => {
  return <Feeds type={2} />
}
